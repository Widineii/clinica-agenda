server.port=${PORT:10000}

spring.datasource.url=jdbc:postgresql://${PGHOST:aws-1-sa-east-1.pooler.supabase.com}:${PGPORT:5432}/${PGDATABASE:postgres}?sslmode=require
spring.datasource.username=${PGUSER:postgres.ykywascubrprjxwnqrua}
spring.datasource.password=${PGPASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

app.seed-demo-data=true
app.seed-admin-login=${ADMIN_LOGIN:}
app.seed-admin-password=${ADMIN_PASSWORD:}
app.seed-admin-name=${ADMIN_NAME:Administracao}
